import WhatsappButton from '../buttons/whatsaap_button'

export default function Header() {
    return (
        <header className="bg-header-primary fixed top-0 flex w-full p-3">
            <WhatsappButton />
        </header>
    )
}
